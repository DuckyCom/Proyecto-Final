@{
    ViewData["Title"] = "Publicaciones de Seguidores";

}

<div class="container">
    <h1 class="text-center">Publicaciones de Seguidores</h1>
<!-- Este for está mal -->
    <!-- @foreach (var post in Model)
{
    <div class="card mb-4 shadow-sm">
        <div class="card-header">
            <h4 class="my-0 font-weight-normal">@post.Nombre</h4>
        </div>
        <div class="card-body">
            <img src="@post.Imagen" class="img-fluid mx-auto d-block" alt="">    
            <p class="card-text">@post.Descripcion</p>
            <div class="d-flex justify-content-between align-items-center">
                <div class="btn-group">
                    <button type="button" id="like-button-@post.ID" class="btn btn-sm btn-outline-secondary" onclick="corazonClick(@post.ID)">&#9825; <span id="like-count-@post.ID">@post.ContadorLikes</span></button>                   
                </div>
            </div>
        </div>
    </div>
} -->


    <div class="d-flex justify-content-between align-items-center mt-4">
        <a href='@Url.Action("Index", "Home")' class="btn btn-danger">Cerrar sesión</a>
        <a href='@Url.Action("Profile", "Home", new { IdUsuario = ViewBag.Usuario.ID })' class="btn btn-primary">Perfil</a>
        <a href='@Url.Action("CrearPubli", "Home", new { IdUsuario = ViewBag.Usuario.ID })' class="btn btn-primary">Crear Publicación</a>
    </div>
</div>
